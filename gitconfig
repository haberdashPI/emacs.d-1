[user]
  name = Antonio Santos
  email = antonio@santosvelasco.com
[color]
  diff = auto
  status = auto
  branch = auto
  blame = auto
[merge]
  tool = vimdiff
[alias]
  aa = add .
  ci = commit
  co = checkout
  slog = log --pretty=format:'%C(white)[%C(yellow)%h%C(white)] %C(blue)(%ad) %C(white)%s %C(green)%an' --date=local
  st = status -s
  blame = blame -w
	wtf = blame -w
[core]
  excludesfile = ~/.gitignore
  preloadindex = true
  editor = emacs
[github]
  user = antonio
[hub]
  protocol = https
[push]
  default = upstream
[credential]
	helper = netrc -f ~/.netrc.gpg -v
[rerere]
	enabled = true
